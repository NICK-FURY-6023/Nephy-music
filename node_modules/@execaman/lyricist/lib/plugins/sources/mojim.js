"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mojim_com = void 0;
const axios_1 = require("axios");
const cheerio_1 = require("cheerio");
async function mojim_com(url, userAgent) {
    try {
        const { data } = await (0, axios_1.get)(url, {
            headers: {
                "User-Agent": userAgent
            }
        });
        const $ = (0, cheerio_1.load)(data);
        const html = $("div[id=frame] > div:has(table) > table td table td > div dd")
            .html()
            ?.replace(/<br>/g, "\n");
        if (typeof html !== "string" || html.length === 0) {
            throw null;
        }
        const lyrics = $(html).text().trim();
        if (lyrics.length === 0) {
            throw null;
        }
        return lyrics;
    }
    catch {
        return Promise.reject();
    }
}
exports.mojim_com = mojim_com;
