"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.lyricsbox_com = void 0;
const axios_1 = require("axios");
const cheerio_1 = require("cheerio");
async function lyricsbox_com(url, userAgent) {
    try {
        const { data } = await (0, axios_1.get)(url, {
            headers: {
                "User-Agent": userAgent
            }
        });
        const $ = (0, cheerio_1.load)(data);
        const lyrics = [];
        $("div[id=lyrics] > div[class*=lyrics_part_text]").each((index, element) => {
            lyrics.push($(element).text());
        });
        if (lyrics.length === 0) {
            throw null;
        }
        return lyrics.join("\n\n");
    }
    catch {
        return Promise.reject();
    }
}
exports.lyricsbox_com = lyricsbox_com;
