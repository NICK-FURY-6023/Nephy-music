"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.letras_com = void 0;
const axios_1 = require("axios");
const cheerio_1 = require("cheerio");
async function letras_com(url, userAgent) {
    try {
        const { data } = await (0, axios_1.get)(url, {
            headers: {
                "User-Agent": userAgent
            }
        });
        const $ = (0, cheerio_1.load)(data);
        const lyrics = [];
        $("div[id=js-lyric-content] div[class*=lyric-original]:first > p").each((index, element) => {
            const html = $(element).html()?.replace(/<br>/g, "\n");
            if (typeof html !== "string") {
                throw null;
            }
            const lyric = (0, cheerio_1.load)(html).text().trim();
            if (lyric.length === 0) {
                throw null;
            }
            lyrics.push(lyric);
        });
        if (lyrics.length === 0) {
            throw null;
        }
        return lyrics.join("\n\n");
    }
    catch {
        return Promise.reject();
    }
}
exports.letras_com = letras_com;
